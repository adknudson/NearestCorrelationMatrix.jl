name: format-check

on:
  push:
    branches:
      - 'main'
  pull_request:

jobs:
  runic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: fredrikekre/runic-action@v1
        with:
          version: '1'